Setup Linux in Windows (WSL2):
-----------------------------
**PowerShell (Run as Administrator)**:

>> wsl --install
>> wsl --status
>> wsl --list --online
>> wsl --install -d Ubuntu-22.04
	Enter Username : sathish_ch
	Password: Sathish@123
>> wsl -d Ubuntu
>> sudo apt update && sudo apt upgrade -y
>> wsl --list --verbose

>> wsl -d Ubuntu-22.04
>> exit                 # stop Ubuntu
>> wsl --shutdown       # stop all WSL
>> wsl --status         # WSL details



Docker Install: 
------------------

## 📄 1️⃣ Create Docker install bash file

Create the file:

```bash
nano docker-install.sh
```

Paste this **exact content**:

```bash
#!/bin/bash
set -e

echo "Updating system..."
sudo apt update

echo "Installing prerequisites..."
sudo apt install -y ca-certificates curl gnupg lsb-release

echo "Adding Docker GPG key..."
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

echo "Adding Docker repository..."
echo \
"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | \
sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

echo "Installing Docker Engine and Docker Compose..."
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

echo "Configuring Docker without sudo..."
sudo usermod -aG docker $USER

echo "Docker installation completed."
echo "Please restart Ubuntu or run: newgrp docker"
```

Save:

* **CTRL + O → Enter**
* **CTRL + X**

---

## ▶️ 2️⃣ Execute the bash file

Make it executable:

```bash
chmod +x docker-install.sh
```

Run it:

```bash
./docker-install.sh
```

---

## 🔁 3️⃣ Apply docker group (IMPORTANT)

After script finishes, run **one**:

```bash
newgrp docker
```

**OR**

```bash
exit
```

(close Ubuntu → reopen)

---

## ✅ 4️⃣ Verify Docker

```bash
docker --version
docker compose version
docker run hello-world
```

---

Kafka 8.1 (KRaft) + Schema Registry + Kafka Connect + Control Center.
Kafka handles messaging, Schema Registry manages schemas, Kafka Connect moves data, and Control Center provides the web UI.

>>> docker compose -f docker-compose-kafka.yml up -d






